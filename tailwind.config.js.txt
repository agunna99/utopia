// tailwind.config.js

/** @type {import('tailwindcss').Config} */
export default {
  // This is the most important part. It's the "content" array.
  // It tells Tailwind which files to scan for class names.
  // If the path here is incorrect, Tailwind won't find your classes.
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
